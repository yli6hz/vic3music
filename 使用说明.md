# My Custom Music — 维多利亚3 自定义音乐 Mod（可分享版）

把你喜欢的音乐加进维多利亚3，出现在游戏自带的音乐播放器里。
**无需安装 Python，无需修改任何路径**——Windows 自带的 PowerShell 即可运行，
游戏版本、安装目录、文档目录全部由脚本自动检测。

## 分享者注意（打包前必读）

- `original\` 里是**你自己的曲库**，根目录的 `.ogg` 是你转码产物——
  分享给别人前请清空这两处，并重新运行一次 `一键部署.bat`（会生成空模板）。
- `tools\` 里如有自动下载的 ffmpeg 可以一并打包，也可以不打包（对方首次运行会提示自动下载）。

## 使用者：一键部署

1. 把本文件夹放到任意位置（不需要放进游戏目录）
2. 双击 **`一键部署.bat`**，脚本会自动完成：
   - 定位 Victoria 3 安装目录（游戏目录内自动识别 / Steam 注册表查找）
   - 读取当前游戏版本（如 `1.14.3`），自动写入 `descriptor.mod` 和 `.metadata\metadata.json`
     的兼容版本（`1.14.*`）——**游戏更新后重新跑一次 bat 即可自动适配**
   - 在文档目录 `Paradox Interactive\Victoria 3\mod\` 创建目录联接完成安装
     （兼容 OneDrive 重定向的文档目录；本文件夹仍是唯一数据源）
   - 查找 ffmpeg：系统 PATH → 本文件夹 `tools\` → 常见位置；
     找不到时询问是否**自动下载到 `tools\`**（仅首次，之后随文件夹保存）
   - 扫描 `original\`，非 `.ogg` 自动转码，生成曲目定义、播放器分类、中英文曲名
3. 把你的音乐文件丢进 `original\` 子文件夹（`.m4a` `.mp3` `.flac` `.wav` `.aac` `.wma` `.ogg` 均可），
   再双击一次 `一键部署.bat`
4. 打开游戏启动器 → 播放集（Playset），勾选 **My Custom Music**
5. 进入游戏后，点右下角音乐播放器，选择 **我的自定义音乐** 分类即可只听自己的歌

## 转码说明（ffmpeg）

- 输出为 OGG Vorbis q6（音质好、体积小），存放在 mod 根目录（游戏的
  `file:/` 引用不支持子目录，所以必须放根目录）。
- 已转码过的文件自动跳过，不会重复转码。
- `original\` 里的源文件**不会被删除或修改**，就是你的曲库备份。
- 极少数损坏文件会转码失败并被列出；改名为 `xxx.m4a.corrupt` 可永久跳过。
- 没有ffmpeg 时也可以：把 `.ogg` 直接放进 `original\`，脚本会原样复制，无需转码。

## 目录结构

```
本文件夹\
├─ original\                          ← 音乐源文件都放这里
├─ tools\                             ← 自动下载的 ffmpeg 存放处（可选）
├─ *.ogg                              ← 转码产物（自动维护，别手动动）
├─ .metadata\metadata.json            启动器元数据（版本号自动更新）
├─ descriptor.mod                     版本兼容声明（自动更新）
├─ v3_music_tool.ps1                  核心脚本（检测+安装+转码+生成）
├─ 一键部署.bat                        一键运行
├─ 使用说明.md                        本文件
├─ music\
│  ├─ my_custom_music.txt             曲目定义（脚本生成）
│  └─ music_player_categories\        播放器分类（脚本生成）
└─ localization\                      中英文曲名（脚本生成）
```

## 常见问题

- **游戏更新后 mod 报版本不兼容？** 重新双击一次 `一键部署.bat`，
  脚本会读取新版本号自动更新兼容声明。
- **游戏内曲名怎么显示？** 源文件名去掉扩展名、下划线还原为空格。
  想改显示名就重命名 `original\` 里的源文件后重新跑 bat。
- **官方原声会被覆盖吗？** 不会；随机播放会混在一起，想只听自己的歌
  就切到"我的自定义音乐"分类。
- **以后加歌/删歌？** 加：放进 `original\` 重跑 bat；删：从 `original\`
  删除源文件并删掉根目录对应 `.ogg`，重跑 bat。
- **卸载**：启动器取消勾选即可；彻底卸载就删掉文档目录 mod 下的
  `my_custom_music` 联接和本文件夹。
- **脚本被杀毒软件拦截？** 脚本只在本机做文件转码和创建目录联接，
  无联网上传；允许运行即可（bat 已使用 -ExecutionPolicy Bypass）。
